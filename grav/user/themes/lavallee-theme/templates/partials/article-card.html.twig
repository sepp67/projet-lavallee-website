<div class="flag note">
  <span class="flag-tag">{{ 'THEME.ARTICLE.CARD_TAG'|t }}</span>
  <h3>{{ article.title }}</h3>
  <p>{{ article.header.subtitle }}</p>
  <a href="{{ article.url }}" class="flag-link">{{ 'THEME.ARTICLE.READ_MORE'|t }}</a>
</div>
